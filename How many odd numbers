#include <iostream>

using namespace std;

int main()
{
    /*
        EXERCISE:
        1) Check if the number is odd
        2) Write as many odd numbers one after another as the user requests.
           user input: 7
           output: 1, 3, 5, 7, 9, 11, 13
		3) Number the odd numbers
        1. = 1
        2. = 3
        3. = 5
        4. = 7
        5. = 9
        6. = 11
        7. = 13


        %

        1 % 2 == 1
        2 % 2 == 0
        3 % 2 == 1
        4 % 2 == 0
        5 % 2 == 1

    */
    int howManyOddNumbers;
    int index = 1;

    cout << "How many odd numbers do you want me to generate?" << endl;

    cin >> howManyOddNumbers;
/*

    for (int i = 0; i < howManyOddNumbers * 2; i++)
    {
        if (i % 2 == 1)
            cout << index++ << ".= " << i << endl;
    }
*/
    for (int i = 1; i < howManyOddNumbers * 2; i+=2)
    {
            cout << index++ << ".= " << i << endl;
    }
    return 0;
}
